---
layout: base.njk
section: page
---
<div class="page-container">
  <h1 class="cv-title {{ 'd-print-none' if '/sobre-mi' in page.url }}">
  {{ title }}</h1>
  {% if subtitle %}
    <p>{{ subtitle }}</p>
  {% endif %}
  <hr class="cv-title {{ 'd-print-none' if '/sobre-mi' in page.url }}">
  {{ layoutContent | safe }}
</div>
